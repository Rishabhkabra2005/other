# Supabase: Project Settings → Database
#
# DATABASE_URL — Connection pooling (Transaction mode), e.g. port 6543
#   postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:6543/postgres?pgbouncer=true
#
# DIRECT_URL — Direct connection (Session mode), e.g. port 5432
#   postgresql://postgres.[project-ref]:[password]@aws-0-[region].pooler.supabase.com:5432/postgres

DATABASE_URL=""
DIRECT_URL=""

NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="change-this-to-a-long-random-secret-in-production"
NEXT_PUBLIC_APP_URL="http://localhost:3000"

# Email — Resend (free tier) or SMTP (e.g. Gmail)
RESEND_API_KEY=""
RESEND_FROM="onboarding@resend.dev"
EMAIL_FROM="CareConnect Health <onboarding@resend.dev>"
SMTP_HOST=""
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER=""
SMTP_PASS=""

# Twilio: Console → Account → API keys & credentials
TWILIO_ACCOUNT_SID=""
TWILIO_AUTH_TOKEN=""
TWILIO_PHONE_NUMBER=""

# OpenAI: Platform → API keys (for AI prescription summaries)
OPENAI_API_KEY=""
